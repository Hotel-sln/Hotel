
<?php
    define('SERVER', 'localhost');
    define('NAME', 'hotel_menton');
    define('USER', 'sam');
    define('PASSWD', 'root');

    $server = SERVER;
     $name = NAME; 
     $user = USER; 
     $passwd = PASSWD;